{
  "name": "openwater",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "An interactive ocean, built with Three.js and original water shaders.",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build && node scripts/build-meta.mjs",
    "preview": "vite preview --host 127.0.0.1",
    "check": "node --check src/main.js && node --check src/world.js && node --check src/water.js && node --check server.mjs",
    "test": "node --test tests/*.test.mjs",
    "start": "node server.mjs"
  },
  "dependencies": {"three": "0.186.0"},
  "devDependencies": {"vite": "8.3.0"},
  "engines": {"node": ">=22.12.0"}
}
